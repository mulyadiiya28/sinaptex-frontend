# API engine (Sinaptex — Node.js/Express). Default sesuai README engine: port 4000
NEXT_PUBLIC_API_URL=http://localhost:4000

# Supabase (dipakai untuk Auth di client — engine hanya verifikasi access token)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
